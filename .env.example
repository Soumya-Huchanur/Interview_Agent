# API Keys Example
# Copy this file to .env and replace with your actual keys

# Google Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Credentials JSON (copy the full JSON content from your service account key)
FIREBASE_CREDENTIALS_JSON={"type": "service_account", "project_id": "your_project_id", ...}
